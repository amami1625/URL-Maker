import styled from "styled-components";

const HeaderElm = styled.header``;

const Header = () => {
  return (
    <HeaderElm>
      <h1>URLメーカー(β)</h1>
    </HeaderElm>
  );
};

export default Header;
